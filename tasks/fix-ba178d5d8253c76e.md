# fix-ba178d5d8253c76e — Fix: Failing dialog spec: /myboard shows empty board when no ships placed

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 SBOT

**Severity:** high

The Tests-gate replay harness reported spec "/myboard shows empty board when no ships placed" as failing. 25 spec(s) failed: [Quick Match button initiates matchmaking deep link with nonexistent invite code returns error deep link with own invite code returns error deep link flow creates pending match and notifies both players regular /start without invite prefix still shows welcome full flow: fire pass turn then /turnt notifies opponent with Your Turn button create match and /turnt while waiting shows not in progress /endgame with no active game /endgame after all ships sunk shows victory with rating update /start replies with welcome and main menu /help lists available commands /placeship with overlapping ships returns overlap error /placeship duplicate ship type returns error /myboard shows empty board when no ships placed /myboard shows board with ships after placement /checkinvite shows invite details for a valid code /newmatch creates a match in waiting state with correct fields /mymatch shows no active matches when none exist create match then query it with /mymatch /fire without active match shows error /history without active match shows error /fire with missing coordinates shows usage /fire with invalid coordinates shows error full move flow: create match, fire, record move, check history /profile without registering shows not-registered message]

## Dialog tests

This is a FIX task: the behavior it repairs is already covered by an existing spec under `tests/specs/`. Fix the code to make that existing spec pass — do NOT author a new `tests/specs/fix-ba178d5d8253c76e.json` (a duplicate spec for the same behavior makes the tests-gate count it twice and it can never go green). Add a new spec file ONLY if you are introducing genuinely new user-facing behavior that no existing spec covers; if so, name it `tests/specs/fix-ba178d5d8253c76e.json` (and any new command `tests/commands/fix-ba178d5d8253c76e.json`).


## Handler module

This is a FIX task. Find the EXISTING handler under `src/handlers/` that implements the affected command/behavior and EDIT it in place. Do NOT create a new `src/handlers/fix-ba178d5d8253c76e.ts` — a second `Composer` binding the same command conflicts with the original and breaks the bot. Create a new handler file ONLY if the affected command does not exist anywhere yet (then name it `src/handlers/fix-ba178d5d8253c76e.ts` and default-export a grammY `Composer`; `buildBot()` auto-loads it). NEVER edit `src/bot.ts`; the global error boundary + unknown-command fallback already live in `buildBot()`.


## Implementation contract

Ship a COMPLETE, working implementation — not a stub. A task is INCOMPLETE (and will be rejected) even if it compiles and the dialog tests pass when it does any of these:
- **Stubbed code:** empty bodies, `TODO`/`FIXME`, commented-out logic, or `throw new Error("not implemented")`.
- **Fabricated data:** `Math.random()`, hardcoded sample arrays, or canned responses standing in for real computed or fetched values.
- **No in-memory data store:** a `Map`/array/module-level variable used as a database is a defect. Anything that must survive a restart (records, subscriptions, balances, schedules, settings) MUST use the toolkit's persistent storage (Redis-backed), not process memory. (The toolkit's auto-selected session storage is only for ephemeral conversation state.)
- **Broken integrations:** call external APIs against their real contract — correct endpoints, ids and params (e.g. a coin *id* like `the-open-network`, not a ticker like `TON`) — with credentials read from env. Do not invent endpoints or fake responses.
- **Dead code:** the feature's command/handler must be registered via its default-exported `Composer` in `src/handlers/<slug>.ts` (auto-loaded) and reachable from the bot's command surface.
If the spec is genuinely under-specified, implement the smallest REAL slice you can verify and note the gap — never fake behavior to make the PR look complete.
